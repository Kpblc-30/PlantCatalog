version: '3.8'
services:
  app:
    build: .
    ports:
      - "5027:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/plants.db
    volumes:
      - plantdata:/app/data

volumes:
  plantdata: